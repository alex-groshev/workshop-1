namespace Domain
{
    // Probably it's not the best name, but we already have 'User' class
    // and I don't want to touch it yet.
    public class BaseUser
    {
        protected int Id;

        protected string Name;
    }
}